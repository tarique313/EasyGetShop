<strong><%= comment.user.name %></strong> commented <%= time_ago_in_words(comment.created_at) %> ago:
</br><p><%= comment.content%></p>
<%= link_to 'Delete', [@order, comment], :method => :delete, :confirm => "Are you sure?" %>