<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h4>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">

  <div class="form-group">)(
    <%= f.text_area :body, class: "col-md-6 col-md-offset-3", placeholder: "আপনার আলীএক্সপ্রেস অর্ডারের লিঙ্ক এখানে পেস্ট করুন। (Paste the Aliexpress Order Url here.)" %>
  </div>
    
</div>
  <!-- <div class="field">
    <%#= f.label :status %>
    <%#= f.check_box :status %>
  </div> -->
<br>
  <div class="form-group">
  <center><%= f.submit class: 'btn btn-primary ', text: "অর্ডার করুন" %></center>
  </div>
  
<% end %>

