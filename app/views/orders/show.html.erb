<p id="notice"><%= notice %></p>
<div class="well">
<p>
  <strong>Order:</strong>
  <%= @order.body %>
</p>
</div>
<hr>
<%= render @order.comments %>(<%= @order.comments.count%>)
<br>
<%= render "comments/form"%>


<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
